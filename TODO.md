# TODO

- [ ] Create plan for making `setInterval` run every 2500ms
- [ ] Update `src/Themes/Banner.jsx` so its interval is consistently 2500ms and avoids stale state issues if needed
- [ ] Run frontend lint/build/tests (npm scripts) to confirm no runtime errors

